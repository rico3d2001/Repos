apiVersion: v1
kind: Service
metadata:
  name: {{ template "brassmateriaisapipqgestao.fullname" . }}
  labels:
    app: {{ template "brassmateriaisapipqgestao.name" . }}
    chart: {{ template "brassmateriaisapipqgestao.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "brassmateriaisapipqgestao.name" . }}
    release: {{ .Release.Name }}
